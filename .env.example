# SentimentLab Environment Configuration
# No secrets or API keys are required for the standalone ML inference engine.
# For optional custom configurations:

PORT=3000
PYTHON_API_URL=http://127.0.0.1:8000
NODE_ENV=production
